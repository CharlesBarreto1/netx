# device-gateway — copie para .env (NUNCA commite .env).

REDIS_URL=redis://localhost:6379
LOG_LEVEL=INFO
CONCURRENCY=4

# Chave-mestra AES-256 do cofre (32 bytes em base64). SOMENTE este serviço a possui (§4).
# Gere uma por instância com:  openssl rand -base64 32
MASTER_KEY=
